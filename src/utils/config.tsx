/**
 * Constants for configuring the behavior of the program.
 */

export const MAX_SEARCH_DEPTH = 4;
export const AI_INTELLIGENCE_FACTOR = 0.9; // Value from 0-1 that represents the odds the AI will use each newly discovered better move (as opposed to ignoring it);

export const blackIsHuman = false;
export const whiteIsHuman = false;
export const AI_MOVE_DELAY = 1000;
